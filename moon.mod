name = "moonbit-community/moon_secure_codec"
version = "0.1.0"
readme = "README.md"
repository = "https://github.com/moonbit-community/moon_secure_codec"
license = "Apache-2.0"
keywords = ["supply-chain", "fingerprint", "merkle", "codec", "crypto", "sbom", "hash"]
description = "A production-grade software supply chain security, multi-base codec and Merkle tree verification toolkit in MoonBit."

options(
  "preferred-target": "wasm-gc",
)
