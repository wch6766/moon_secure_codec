name = "moonbit-community/moon_datalog"
version = "0.1.0"
readme = "README.mbt.md"
repository = "https://github.com/moonbit-community/moon_datalog"
license = "Apache-2.0"
keywords = ["datalog", "relational-algebra", "deductive-database", "query-engine", "graph"]
description = "A production-grade, high-performance in-memory Datalog deductive query engine and relational algebra processor in MoonBit."

options(
  "preferred-target": "native",
)
